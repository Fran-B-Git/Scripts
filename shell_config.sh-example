#!/bin/sh
# This is an example configuration
# Rename this file to `shell_config.sh` and move it to `/config/shell_config.sh`
# Set the sandboxBaseUrl to the sandbox url you want to check
sandboxBaseUrl=##SBDOMAIN##
user="gast"
password="creative"
export user
export password
threadCount=2
